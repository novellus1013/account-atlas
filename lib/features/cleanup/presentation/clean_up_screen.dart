import 'package:flutter/material.dart';

class CleanUpScreen extends StatelessWidget {
  const CleanUpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('clean_up_screen'));
  }
}
